[project]
name = "gauntlet"
version = "0.1.0"
description = "CLI eval harness for local LM Studio models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "rich>=14.2.0",
    "sqlite-utils>=3.39",
]
